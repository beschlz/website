import type { GatsbyConfig } from "gatsby";

const config: GatsbyConfig = {
  siteMetadata: {
    title: `lebenslauf`,
    siteUrl: `https://www.bschulz.dev`
  },
  graphqlTypegen: false,
};

export default config;
